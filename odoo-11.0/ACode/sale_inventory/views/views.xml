<odoo>
  <data>
    <!-- explicit list view definition -->
    <!--
    <record model="ir.ui.view" id="sale_inventory.list">
      <field name="name">sale_inventory list</field>
      <field name="model">sale_inventory.sale_inventory</field>
      <field name="arch" type="xml">
        <tree>
          <field name="name"/>
          <field name="value"/>
          <field name="value2"/>
        </tree>
      </field>
    </record>
    -->

    <!-- actions opening views on models -->
    <!--
    <record model="ir.actions.act_window" id="sale_inventory.action_window">
      <field name="name">sale_inventory window</field>
      <field name="res_model">sale_inventory.sale_inventory</field>
      <field name="view_mode">tree,form</field>
    </record>
    -->

    <!-- server action to the one above -->
    <!--
    <record model="ir.actions.server" id="sale_inventory.action_server">
      <field name="name">sale_inventory server</field>
      <field name="model_id" ref="model_sale_inventory_sale_inventory"/>
      <field name="state">code</field>
      <field name="code">
        action = {
          "type": "ir.actions.act_window",
          "view_mode": "tree,form",
          "res_model": self._name,
        }
      </field>
    </record>
    -->

    <!-- Top menu item -->
    <!--
    <menuitem name="sale_inventory" id="sale_inventory.menu_root"/>
    -->
    <!-- menu categories -->
    <!--
    <menuitem name="Menu 1" id="sale_inventory.menu_1" parent="sale_inventory.menu_root"/>
    <menuitem name="Menu 2" id="sale_inventory.menu_2" parent="sale_inventory.menu_root"/>
    -->
    <!-- actions -->
    <!--
    <menuitem name="List" id="sale_inventory.menu_1_list" parent="sale_inventory.menu_1"
              action="sale_inventory.action_window"/>
    <menuitem name="Server to list" id="sale_inventory" parent="sale_inventory.menu_2"
              action="sale_inventory.action_server"/>
    -->
  </data>
</odoo>