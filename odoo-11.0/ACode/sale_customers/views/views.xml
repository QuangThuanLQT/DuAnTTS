<odoo>
  <data>
    <!-- explicit list view definition -->
    <!--
    <record model="ir.ui.view" id="sale_customers.list">
      <field name="name">sale_customers list</field>
      <field name="model">sale_customers.sale_customers</field>
      <field name="arch" type="xml">
        <tree>
          <field name="name"/>
          <field name="value"/>
          <field name="value2"/>
        </tree>
      </field>
    </record>
    -->

    <!-- actions opening views on models -->
    <!--
    <record model="ir.actions.act_window" id="sale_customers.action_window">
      <field name="name">sale_customers window</field>
      <field name="res_model">sale_customers.sale_customers</field>
      <field name="view_mode">tree,form</field>
    </record>
    -->

    <!-- server action to the one above -->
    <!--
    <record model="ir.actions.server" id="sale_customers.action_server">
      <field name="name">sale_customers server</field>
      <field name="model_id" ref="model_sale_customers_sale_customers"/>
      <field name="state">code</field>
      <field name="code">
        action = {
          "type": "ir.actions.act_window",
          "view_mode": "tree,form",
          "res_model": self._name,
        }
      </field>
    </record>
    -->

    <!-- Top menu item -->
    <!--
    <menuitem name="sale_customers" id="sale_customers.menu_root"/>
    -->
    <!-- menu categories -->
    <!--
    <menuitem name="Menu 1" id="sale_customers.menu_1" parent="sale_customers.menu_root"/>
    <menuitem name="Menu 2" id="sale_customers.menu_2" parent="sale_customers.menu_root"/>
    -->
    <!-- actions -->
    <!--
    <menuitem name="List" id="sale_customers.menu_1_list" parent="sale_customers.menu_1"
              action="sale_customers.action_window"/>
    <menuitem name="Server to list" id="sale_customers" parent="sale_customers.menu_2"
              action="sale_customers.action_server"/>
    -->
  </data>
</odoo>